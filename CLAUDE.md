# 微信小程序游戏开发要点

## 项目结构

```
miniprogram/
├── app.js / app.json / app.wxss    # 入口、路由、全局样式
├── project.config.json              # 开发者工具配置（AppID、libVersion 等）
├── utils/                           # 纯逻辑，与 UI 无关
│   ├── game-core.js                  # 游戏核心（棋盘操作、移动、胜负判定）
│   ├── storage.js                    # 数据持久化（分数、排行榜、统计）
│   └── sound.js                      # 音效（程序化 WAV + 触觉反馈）
└── pages/
    ├── game/                         # 游戏主界面
    ├── leaderboard/                  # 排行榜
    └── stats/                        # 数据统计
```

## 核心技术要素

### 1. 游戏逻辑与 UI 严格分离
- 核心逻辑（`game-core.js`）用纯函数实现：输入棋盘 + 方向 → 输出新棋盘 + 得分 + 是否移动
- 不依赖 `wx.*` API，可独立测试
- UI 层（Page）只负责渲染和事件转发

### 2. 触摸交互
- **全屏触摸**：在最外层容器绑定 `catchtouchstart/move/end`，不要只在棋盘区域绑定，否则手机上操作不便
- 用 `catch` 前缀（非 `bind`）阻止事件冒泡和页面滚动
- 滑动方向判断：比较 dx/dy 绝对值，取较大轴作为方向
- 设置最小滑动阈值（如 30px），过滤误触

### 3. 动画系统（网格布局下）
- 网格布局（`display: flex` + `aspect-ratio: 1`）比绝对定位更适配不同屏幕，用 rpx 单位
- 动画通过比对 **移动前后的棋盘状态** 确定每个格子的动画类型：
  - `old=0, new>0` → 新方块弹入动画
  - `old>0, new>=old*2` → 合并弹跳动画
  - `new>=256` → 加大合并力度 + 亮度变化
- CSS `@keyframes` 定义动画，动态 className 触发，`setTimeout` 清除

### 4. 音效与触觉反馈
- 小程序无 DOM Audio API，需用 `wx.createInnerAudioContext()`
- 可**程序化生成 WAV**（44 字节头 + PCM 数据），写入 `wx.env.USER_DATA_PATH` 后播放，无需外挂音频文件
- 搭配 `wx.vibrateShort()` 触觉反馈（`light`/`medium`/`heavy`）
- 不同事件使用不同频率/时长区分音色

### 5. 数据持久化
- `wx.getStorageSync/setStorageSync` 存储分数、游戏记录
- 游戏结束时保存记录（非每次操作），避免频繁写入
- 排行榜：历史记录排序取 Top 20
- 统计面板：总场次、最高分、平均分、最近 N 场

### 6. 页面导航
- `app.json` 中配置 `tabBar`，3 个以内标签页体验最佳
- 排行榜和统计页在 `onShow` 中刷新数据（切换标签页时更新）

### 7. 暂停与状态管理
- 右上角暂停按钮用 `catchtap` 阻止事件冒泡
- 暂停时设置 `paused` 状态，触摸事件中检查该状态提前 return
- 暂停弹窗覆盖层点击可关闭

## 用户反馈驱动的迭代要点

### 第一次反馈及修改：
1. **"只有上半部分可触摸"** → 触摸事件从棋盘提升到全屏容器，解决操作不便
2. **"加入动画"** → 引入 prevBoard 对比机制 + CSS keyframes 三类动画（弹出/合并/大合并）
3. **"加入音效"** → 程序化生成 WAV 音效 + 震动反馈，无需外部音频资源

### 通用启示：
- 手机上的游戏**必须全屏可操作**，局部触控区域会让体验大打折扣
- 动画不是装饰，是**操作反馈**——玩家通过动画确认每次滑动是否有变化
- 音效能极大提升手感，程序化生成是避免外部依赖的好方案
- 纯函数式的核心逻辑让加动画（比对前后状态）变得简单
